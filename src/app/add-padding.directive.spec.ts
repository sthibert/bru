import { AddPaddingDirective } from './add-padding.directive';

describe('AddPaddingDirective', () => {
  it('should create an instance', () => {
    const directive = new AddPaddingDirective();
    expect(directive).toBeTruthy();
  });
});
